info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{HOST}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "sidinei.silva@teste.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
