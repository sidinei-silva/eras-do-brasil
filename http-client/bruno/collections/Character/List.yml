info:
  name: List
  type: http
  seq: 2

http:
  method: GET
  url: "{{HOST}}/character"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
