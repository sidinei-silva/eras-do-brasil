info:
  name: Create Account
  type: http
  seq: 1

http:
  method: POST
  url: "{{HOST}}/accounts"
  body:
    type: json
    data: |-
      {
        "email": "sidinei.silva@teste.com",
        "password": "12345678",
        "confirm_password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
