info:
  name: Me
  type: http
  seq: 2

http:
  method: GET
  url: "{{HOST}}/me"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
